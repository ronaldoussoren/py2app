Minimal typing stubs for libraries used by pyapp.

These stubs are far from complete, just the minimal
needed to get good enough typing checking for  py2app
itself.

Note: Only add stubs here for libraries that
don't have type info available, prefer installing
those libraries in tox.ini.
